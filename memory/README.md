# Local Project Memory

This directory is intentionally kept out of version control.

Jarvis can store project-scoped notes here for the current checkout, but these files may contain private or user-specific context and should not be pushed to GitHub.

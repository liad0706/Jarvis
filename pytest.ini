[pytest]
testpaths = tests
asyncio_mode = auto
python_files = test_*.py
python_classes = Test*
python_functions = test_*
timeout = 30
markers =
    live: requires real hardware or live external services (deselect with -m "not live")
    integration: requires Ollama or other local services to be running
